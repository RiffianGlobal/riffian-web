{
  "name": "@riffian-web/core",
  "version": "0.0.1",
  "scripts": {
    "build": "tsup src/index.ts --outDir ./lib --format esm,cjs --dts --clean",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "localforage": "^1.10.0",
    "readable-stream": "^4.4.2",
    "utilShim": "npm:util@^0.12.5"
  },
  "peerDependencies": {
    "lit": "^2",
    "tsup": "^8"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/readable-stream": "^2.3.15"
  }
}
